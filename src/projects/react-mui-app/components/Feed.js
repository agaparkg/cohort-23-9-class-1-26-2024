import { Typography } from "@mui/material";
import React from "react";

const Feed = () => {
  return (
    <Typography variant="h4" component="p">
      Feed
    </Typography>
  );
};

export default Feed;
